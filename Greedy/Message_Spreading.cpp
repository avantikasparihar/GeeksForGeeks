#include<bits/stdc++.h>

using namespace std;

int main()
 {
	// only gravity will pull me down

	// Message Spreading
    
    int t, n;
    cin >> t;
    while (t--) {
        cin >> n;
        cout << (n-1)*2 << endl;	// also a puzzle on gfg
    }
    
	return 0;
}